var view = {};

/**
 * @method on
 * @static
 * 
 * @param {String} class_name Class name of the object to create.
 * @return {Handle}
 */
view.create = function(class_name) {
	
}

/**
 * @method on
 * @static
 * 
 * @param {ui.Handle} handle Handle of object to attach listener to. 
 * @param {String} event_type Name of the event to attach listener to.
 * @param {Function} cb Callback.
 */
view.on = function(handle, event_type, cb) {
	// Native.
}

/**
 * @method set_bounds
 * @static
 * 
 * @param {ui.Handle} handle
 * @param {Number} left
 * @param {Number} top
 * @param {Number} right
 * @param {Number} bottom
 */
view.set_bounds = function(handle, left, top, right, bottom) {
	// Native.
}